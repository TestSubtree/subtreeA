name: PR Workflow for main

on:
  pull_request:
    branches:
      - main # PR 대상 브랜치가 main일 때만 트리거

jobs:
  check_subtree_changes:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repository A
      - name: Checkout repository A
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      # 2. Clone repository B and remove .git folder
      - name: Clone repository B
        run: |
          git clone --branch main --depth 1 https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/TestSubtree/subtreeC.git repository_database
          rm -rf repository_database/.git

      # 3. Compare subtree changes using git diff
      - name: Check subtree changes
        id: check_changes
        run: |
          git --no-pager diff --exit-code --no-index C repository_database > diff_output.txt || echo "Subtree has changes."
          if [ -s diff_output.txt ]; then
            echo "no_changes=false" >> $GITHUB_ENV
          else
            echo "no_changes=true" >> $GITHUB_ENV
          fi

      # 4. Print message and exit if no changes
      - name: No changes in subtree
        if: env.no_changes == 'true'
        run: echo "수정사항이 없습니다. 워크플로우를 종료합니다."

      # 5. Show detailed changes if they exist
      - name: Subtree changes details
        if: env.no_changes == 'false'
        run: |
          echo "다음과 같은 상세한 수정사항이 있습니다:"
          git --no-pager diff --no-index C repository_database || true
